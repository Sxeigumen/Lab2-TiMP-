#include<string>
int main(){
    std::string name; //Create string
    std::cout << "What is your name?" ; //Output the text
    std::cin >> name; //Entering a string and saving it in "name"
    std::cout << "Hello World from " << name; //Output of the text and the "name" stringS
    return 0;
}
